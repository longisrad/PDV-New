name: Build Padavan for NEWIFI3

on:
  workflow_dispatch: # Cho phép nhấn nút kích hoạt biên dịch thủ công từ giao diện Web

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v3
      with:
        ref: dev # Chỉ định biên dịch nhánh dev của bạn

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt install unzip libtool-bin curl cmake gperf gawk flex bison nano xxd \
            fakeroot kmod cpio git python3-docutils gettext automake autopoint \
            texinfo build-essential help2man pkg-config zlib1g-dev libgmp3-dev \
            libmpc-dev libmpfr-dev libncurses5-dev libltdl-dev wget libc-dev-bin \
            libctf-nobfd0 libcunit1-dev libhdf5-dev libopenblas-dev -y

    - name: Prepare Toolchain and Copy Config Files
      run: |
        # 1. Chép đè file cấu hình bo mạch NEWIFI3 đã tối ưu vào đúng vị trí
        if [ -f "NEWIFI3.config.txt" ]; then
          cp -f NEWIFI3.config.txt trunk/configs/boards/NEWIFI3/board.config
        fi
        
        # 2. Chép đè file cấu hình nhân kernel-3.4.x đã tối ưu vào đúng vị trí
        if [ -f "kernel-3.4.x.config" ]; then
          cp -f kernel-3.4.x.config trunk/configs/boards/NEWIFI3/kernel-3.4.x.config
        fi
        
        # Cấp quyền chạy cho các tệp thực thi chính
        chmod +x trunk/clear_tree
        chmod +x trunk/build_firmware_modify
        chmod +x toolchain-mipsel/dl_toolchain.sh

    - name: Download Prebuilt Toolchain
      run: |
        cd toolchain-mipsel
        sh dl_toolchain.sh

    - name: Compile Padavan Firmware
      run: |
        cd trunk
        sudo ./clear_tree
        fakeroot ./build_firmware_modify NEWIFI3

    - name: Upload Firmware to Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: Padavan_NEWIFI3_D2
        path: trunk/images/*.trx
